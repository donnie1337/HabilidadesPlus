<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rpgcustom</groupId>
    <artifactId>HabilidadesPlus</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>HabilidadesPlus</name>
    <description>Plugin de RPG customizado inspirado no mcMMO, com XP e nivel na action bar e menu via /mcmmo</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--
            Compilamos o bytecode visando o Java 21 (baseline atual do Spigot/Paper).
            Um JAR compilado com release 21 roda normalmente em qualquer JVM igual
            ou mais nova (Java 25, Java 26, etc.), pois a JVM e retrocompativel.
            Se quiser travar especificamente em Java 26, troque os dois valores abaixo para 26,
            mas isso impede o plugin de rodar em servidores com Java mais antigo.
        -->
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <repositories>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/groups/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Spigot API 26.2 (Minecraft 26.2 "Chaos Cubed") -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>26.2-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>

        <!--
            O Spigot API ja traz o Adventure (net.kyori.adventure) transitivamente,
            mas declaramos aqui explicitamente por seguranca/clareza, ja que usamos
            Component, LegacyComponentSerializer e Title diretamente no codigo.
            Escopo "provided": o Spigot/Paper ja fornece isso em tempo de execucao.
        -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-api</artifactId>
            <version>4.17.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-serializer-legacy</artifactId>
            <version>4.17.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.name}-${project.version}</finalName>
        <defaultGoal>clean package</defaultGoal>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
        </plugins>
    </build>
</project>
